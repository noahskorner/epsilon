## Goal

Design a UI for managing search indexes.

## Requirements

- Server-side render the page using the **Find Indexes** API
- Display indexes in a card or list view
- Support pagination with a sensible default page size (50)
