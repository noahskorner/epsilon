## Goal

Design a wizard-style UI for safely creating new indexes.

## Requirements

- Connect the **Create Index** button on the dashboard
- Add a route at `/dashboard/indexes/new`
- Implement a step-by-step wizard/form for index creation
- Use a server action with `create-index-facade.ts` to create the index
